# CLAUDE.md

This configuration file provides guidance to Claude Code (`claude.ai/code`) for this repository.

## Project Guidelines & Protocols
You MUST read and follow the shared project-level protocols defined in:
* `.citadel/agents/PROJECT_PROTOCOLS.md`

## Build and Test Commands
- Build: `npm run build`
- Test: `npm run test:run`
- Lint/Type-check: `npx tsc -b`
- Dev Server: `npm run dev`

## Code Guidelines
- **TypeScript**: Write clean, fully typed TypeScript code. Avoid `any` types.
- **State Management**: Use Zustand + Immer as per the project protocols.
- **Testing**: Ensure all new features have accompanying unit/integration tests (Vitest/Playwright).

## Session & Identity (Citadel / KoadOS Integration)
* **Local Identity Vault**: [Local Profile Vault](file:///[LOCAL_VAULT_PATH])
* **Hydration Command**: `source [PATH_TO_KOAD_FUNCTIONS] && agent-boot [AGENT_NAME_LOWERCASE]`

## Developer Overrides & Custom Rules
This section is reserved for developer-specific rules, local overrides, and custom instructions:
* [Enter custom developer rules here...]
